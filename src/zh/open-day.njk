---
permalink: "/zh/open-day.html"
bodyClass: "page-open-day-hub"
title: "开放日 — EBiM Benchmark"
description: "EBiM 开放日将本基准以线下形式带到社区面前——讲座、机器人现场演示与实验室参访。汉堡站 2026 年 8 月 17 日；上海站 2026 年 8 月 21 日。"
keywords: "EBiM 开放日, 具身双臂操作, 机器人基准, 汉堡, 上海, 慕尼黑, 匹兹堡"
canonical: "https://ebim-benchmark.github.io/zh/open-day.html"
headComments: true
backToTop: true
navActive: "openDay"
i18nKey: openDay
---
{% extends "layouts/base.njk" %}

{% block headStructured %}
  <!-- Open Graph -->
  <meta property="og:type"        content="website" />
  <meta property="og:site_name"   content="EBiM Benchmark" />
  <meta property="og:url"         content="https://ebim-benchmark.github.io/zh/open-day.html" />
  <meta property="og:title"       content="开放日 — EBiM Benchmark" />
  <meta property="og:description" content="EBiM 开放日将本基准以线下形式带到社区面前。汉堡站 2026 年 8 月 17 日；上海站 2026 年 8 月 21 日。" />
  <meta property="og:image"       content="https://ebim-benchmark.github.io/img/og-cover.png" />
  <meta property="og:image:width"  content="1200" />
  <meta property="og:image:height" content="630" />
  <meta property="og:image:alt"   content="EBiM Benchmark — 迈向面向真实世界具身双臂操作的全球协同基准" />
  <meta property="og:locale"      content="zh_CN" />
  <!-- Twitter / X Card -->
  <meta name="twitter:card"        content="summary_large_image" />
  <meta name="twitter:title"       content="开放日 — EBiM Benchmark" />
  <meta name="twitter:description" content="EBiM 开放日将本基准以线下形式带到社区面前。汉堡站 2026 年 8 月 17 日；上海站 2026 年 8 月 21 日。" />
  <meta name="twitter:image"       content="https://ebim-benchmark.github.io/img/og-cover.png" />
  <meta name="twitter:image:alt"   content="EBiM Benchmark — 迈向面向真实世界具身双臂操作的全球协同基准" />
{% include "jsonld.njk" %}
{% endblock %}
{# NOTE: this page IS in the jsonLd whitelist (see _data/eleventyComputed.js), but as a
   HUB it emits Organization + BreadcrumbList only (with the zh jsonld.* strings) —
   never an Event. The dated Events live on the individual Open Day pages, each behind
   its own date gate. It has no meta.<key> entry, so pageMeta is undefined and base.njk
   falls back to the front-matter title/description above. #}

{% block headStyle %}{% raw %}
  <style>
    .fade-in { opacity: 0; transform: translateY(20px); transition: opacity .55s ease, transform .55s ease; }
    .fade-in.visible { opacity: 1; transform: none; }

    /* Active nav link for the current page (Open Day → the hub) */
    .page-open-day-hub .nav-links a[href="open-day.html"] { color: var(--accent); font-weight: 700; }

    /* Back to top */
    #back-to-top {
      position: fixed; bottom: 2rem; right: 1.5rem; width: 44px; height: 44px; border-radius: 50%;
      background: var(--accent, #00d4ff); color: #0a0e1a; border: none; cursor: pointer;
      display: flex; align-items: center; justify-content: center;
      opacity: 0; visibility: hidden; transform: translateY(12px);
      transition: opacity .3s, visibility .3s, transform .3s, background .2s, box-shadow .2s;
      z-index: 998; box-shadow: 0 4px 18px rgba(0,212,255,.35);
    }
    #back-to-top.visible { opacity: 1; visibility: visible; transform: none; }
    #back-to-top:hover { background: #fff; box-shadow: 0 6px 24px rgba(0,212,255,.45); }
    #back-to-top:focus-visible { outline: 2px solid #00d4ff; outline-offset: 3px; }
    #back-to-top svg { pointer-events: none; }

    /* ── Sub-page hero (copied from open-day-hamburg.njk) ── */
    #home.subhero {
      min-height: 32vh; display: flex; align-items: center; justify-content: center;
      position: relative; background: var(--bg-dark); overflow: hidden;
      padding: calc(var(--nav-h) + 2.5rem) 0 3rem; box-sizing: border-box;
    }
    #home.subhero::before {
      content: ''; position: absolute; inset: 0;
      background-image:
        linear-gradient(rgba(0,212,255,.07) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0,212,255,.07) 1px, transparent 1px);
      background-size: 60px 60px; pointer-events: none;
    }
    #home.subhero::after {
      content: ''; position: absolute; inset: 0;
      background: radial-gradient(ellipse 80% 60% at 50% 40%, rgba(0,212,255,.10) 0%, transparent 70%);
      pointer-events: none;
    }
    .subhero-content { position: relative; z-index: 1; text-align: center; max-width: 820px; padding: 1.25rem 1.5rem; }
    .breadcrumb { display: inline-block; font-size: .78rem; letter-spacing: .14em; text-transform: uppercase; color: rgba(255,255,255,.55); margin-bottom: 1rem; }
    .breadcrumb a { color: rgba(255,255,255,.55); transition: color .2s; }
    .breadcrumb a:hover { color: var(--accent); }
    .breadcrumb-sep { margin: 0 .55rem; opacity: .5; }
    .breadcrumb-current { color: var(--accent); }
    .subhero-title { font-size: clamp(1.7rem, 3.6vw, 2.6rem); color: #fff; font-weight: 800; line-height: 1.18; margin-bottom: .9rem; letter-spacing: -.01em; }
    .subhero-subtitle { font-size: clamp(.95rem, 1.6vw, 1.1rem); color: rgba(255,255,255,.72); margin-bottom: .8rem; max-width: 660px; margin-left: auto; margin-right: auto; }
    @media (max-width: 700px) { #home.subhero { min-height: 46vh; padding-top: calc(var(--nav-h) + 1.5rem); padding-bottom: 2rem; } }

    /* Hero eyebrow. Unlike open-day-hamburg.njk this forces its OWN line: the
       pill is shrink-to-fit and the breadcrumb above it is inline-block, so a
       SHORT eyebrow ("开放日") rides up beside the breadcrumb and renders as
       "首页 / 开放日 [开放日]" on one cramped line. Hamburg only escapes that
       because its eyebrow text is long enough to wrap on its own.
       block + max-content keeps the pill hugging its text AND breaks the line. */
    .od-eyebrow {
      display: block; width: max-content; max-width: 100%;
      margin: 0 auto 1rem; padding: .3rem .9rem; border-radius: 100px;
      background: rgba(0,212,255,.10); border: 1px solid rgba(0,212,255,.35);
      font-size: .76rem; font-weight: 700; letter-spacing: .06em; text-transform: uppercase; color: var(--accent);
    }

    /* ── The Open Day cards ── */
    .odh-grid {
      display: grid; grid-template-columns: repeat(auto-fit, minmax(310px, 1fr));
      gap: 1.75rem; margin-top: 2.5rem;
    }
    .odh-card {
      display: flex; flex-direction: column; gap: .7rem;
      background: var(--bg-white); border: 1px solid var(--border); border-radius: 16px;
      padding: 2rem 1.75rem; box-shadow: var(--card-shadow);
      transition: transform .25s ease, box-shadow .25s ease, border-color .25s ease;
    }
    .odh-card:hover { transform: translateY(-4px); border-color: rgba(0,212,255,.45); box-shadow: 0 10px 30px rgba(10,14,26,.08); }
    .odh-city { font-size: 1.15rem; font-weight: 800; color: var(--bg-dark); line-height: 1.3; }
    .odh-meta { font-size: .85rem; font-weight: 600; color: var(--accent-dark); line-height: 1.55; }
    .odh-desc { font-size: .95rem; line-height: 1.65; color: var(--text-mid); }
    .odh-link { margin-top: auto; padding-top: .6rem; font-size: .95rem; font-weight: 700; color: var(--accent-dark); }
    .odh-link:hover { text-decoration: underline; }
    @media (max-width: 540px) { .odh-card { padding: 1.5rem 1.25rem; } }

    .odh-more { max-width: 860px; margin: 2.25rem auto 0; text-align: center; font-size: .92rem; color: var(--text-muted); line-height: 1.65; }

    /* Clarifier callout — Open Days are NOT the Phase III workshop */
    .od-callout {
      display: flex; align-items: flex-start; gap: .7rem; max-width: 860px; margin: 2.5rem auto 0;
      background: rgba(245,158,11,.07); border: 1px solid rgba(245,158,11,.32); border-radius: 12px;
      padding: 1rem 1.2rem; font-size: .92rem; color: var(--text-mid); line-height: 1.6;
    }
    .od-callout svg { flex-shrink: 0; margin-top: .15rem; color: #b45309; }

    .od-contact-line { max-width: 760px; margin: 2.5rem auto 0; text-align: center; font-size: .92rem; color: var(--text-muted); line-height: 1.6; }
    .od-contact-line a { color: var(--accent-dark); font-weight: 600; }
  </style>
{% endraw %}{% endblock %}

{% block main %}
<main id="main-content">

<!-- ============================================================
     HERO
     ============================================================ -->
{% raw %}
<section id="home" class="subhero">
  <div class="subhero-content">
    <div class="hero-wordmark hero-wordmark-sm" role="img" aria-label="EBiM Benchmark">
      <span class="hero-wordmark-mark">EBiM</span>
      <span class="hero-wordmark-sub">Benchmark</span>
    </div>

    <div class="breadcrumb" aria-label="Breadcrumb">
      <a href="index.html">首页</a>
      <span class="breadcrumb-sep">/</span>
      <span class="breadcrumb-current">开放日</span>
    </div>

    <div class="od-eyebrow">开放日</div>

    <h1 class="subhero-title">EBiM 开放日</h1>
    <p class="subhero-subtitle">EBiM 开放日将本基准以线下形式带到社区面前——由特邀讲者与基准团队成员带来讲座、在 Mobile FR3 Duo 平台上的现场演示，以及参观真正开展实机工作的实验室与评测中心。上海场次落在第二阶段的实机调试窗口内；汉堡场次则更早，就在第二阶段开始前几天。</p>
  </div>
</section>
{% endraw %}

<!-- ============================================================
     THE OPEN DAYS — one card per dated day, in date order
     ============================================================ -->
<section id="open-days">
  <div class="container">
    <div class="text-center fade-in">
      <h2 class="section-title">即将举行的开放日</h2>
    </div>

    <div class="odh-grid fade-in">

      <div class="odh-card">
        <div class="odh-city">汉堡——2026 年 8 月 17 日（周一）</div>
        <div class="odh-meta">Google 汉堡办公室 · 10:30–17:00 CEST · 免费，需报名 · IJCAI-ECAI 2026 同期活动</div>
        <p class="odh-desc">讲座、参观 EBiM 评测中心，以及汉堡大学实验室参访。</p>
        <a class="odh-link" href="{{ links.openDayHamburg }}">汉堡开放日 <span aria-hidden="true">→</span></a>
      </div>

      <div class="odh-card">
        <div class="odh-city">上海——2026 年 8 月 21 日（周五）</div>
        <div class="odh-meta">Franka Robotics 上海办公室 · 14:00–17:00（UTC+8） · 凭邀请参加</div>
        <p class="odh-desc">关于机器人教育、医疗健康 AI 与 VLA 机器人真实落地的讲座，以及在 EBiM 上海测试台参观 Mobile FR3 Duo 比赛现场。</p>
        <a class="odh-link" href="{{ links.openDayShanghai }}">上海开放日 <span aria-hidden="true">→</span></a>
      </div>

    </div>

    <p class="odh-more fade-in">慕尼黑与匹兹堡测试台的开放日也在筹备中，日期将在本页公布。</p>

    <div class="od-callout fade-in">
      <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24"
           stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <circle cx="12" cy="12" r="10"/><line x1="12" y1="16" x2="12" y2="12"/><line x1="12" y1="8" x2="12.01" y2="8"/>
      </svg>
      <span>开放日不是 EBiM 第三阶段研讨会。研讨会——特邀报告、圆桌讨论、海报与颁奖典礼——是另一场独立活动，其日期与地点仍待公布。</span>
    </div>

    <p class="od-contact-line fade-in">
      对开放日有疑问？<a href="{{ links.contact }}">与我们联系 <span aria-hidden="true">→</span></a>
    </p>
  </div>
</section>

</main>
{% endblock %}
